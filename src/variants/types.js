/**
 * Variant ID type - one of the supported variant identifiers
 * @typedef {"prod" | "v1" | "v2" | "v3" | "v4"} VariantId
 */

/**
 * Variant configuration interface
 * @typedef {Object} VariantConfig
 * @property {boolean} canEdit - Whether editing is enabled
 * @property {boolean} enableCropping - Whether cropping is enabled
 * @property {boolean} enableCoverSelection - Whether cover selection is enabled
 * @property {"none" | "top" | "bottom"} coverSelectionMode - Cover selection alignment mode
 * @property {boolean} showEditToolbar - Whether to show the edit toolbar
 * @property {boolean} showCroppingTools - Whether to show cropping tools
 * @property {boolean} showCoverSelectionUI - Whether to show cover selection UI
 */

// Export for JSDoc reference (not used at runtime)
export const VariantConfig = {}
