/**
 * Prod - Base configuration (no changes)
 * This is the default production configuration.
 * All variants extend from this base.
 */
export const prodConfig = {
  canEdit: true,
  enableCropping: false,
  enableCoverSelection: false,
  coverSelectionMode: "none",
  showEditToolbar: true,
  showCroppingTools: false,
  showCoverSelectionUI: false,
  enablePreviewFlow: false,
}
