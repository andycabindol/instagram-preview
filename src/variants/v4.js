/**
 * v4 - Cover bottom selection
 * Enables cover selection with bottom alignment
 */
export const v4Config = {
  enableCoverSelection: true,
  showCoverSelectionUI: true,
  coverSelectionMode: "bottom",
  enablePreviewFlow: true,
}
